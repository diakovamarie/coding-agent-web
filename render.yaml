services:
  - type: web
    name: coding-agent-web
    runtime: python
    buildCommand: pip install --no-cache-dir -r requirements.txt && python -c "import app.main; print('import ok')"
    startCommand: python run.py
    envVars:
      - key: OPENAI_API_KEY
        sync: false
